<p align="center">
  <img src="src/assets/logo-light.jpg" alt="BARQ Ai Logo" width="120" />
</p>

<h1 align="center">⚡ BARQ Ai — برق Ai</h1>

<p align="center">
  <strong>أول منصة عربية لبناء المواقع بالذكاء الاصطناعي</strong><br/>
  Arabic-First AI Website Builder — Made for Saudi Arabia 🇸🇦
</p>

<p align="center">
  <a href="https://barqai.site">🌐 الموقع الرسمي</a> •
  <a href="#-المميزات">✨ المميزات</a> •
  <a href="#-التقنيات-المستخدمة">🛠 التقنيات</a> •
  <a href="#%EF%B8%8F-طريقة-العمل-بالتفصيل">⚙️ طريقة العمل</a>
</p>

---

## 📖 نبذة عن المشروع

**BARQ Ai** هو منصة SaaS متكاملة تمكّن المستخدمين من بناء مواقع ويب احترافية بالكامل عن طريق المحادثة باللغة العربية (اللهجة السعودية). المنصة تستخدم نظام **ذكاء اصطناعي مزدوج** (Dual-AI) يفصل بين التخطيط والتنفيذ لتحقيق أعلى جودة ممكنة.

> 💡 **الفكرة:** تكتب وش تبغى بالعربي → الذكاء الاصطناعي يخطط ويبني الموقع كامل قدامك لحظياً.

---

## ✨ المميزات

### 🧠 نظام الذكاء الاصطناعي المزدوج (Dual-AI Engine)
- **المشرف (Supervisor — Gemini 2.5 Flash):** يتولى فهم طلبك بالعربي، يسأل أسئلة توضيحية، ويبني خطة تقنية مفصلة بالإنجليزية
- **المبرمج (Programmer — LLaMA 3.3 70B via Groq):** ينفذ الخطة التقنية ويولّد ملفات React/Tailwind بجودة إنتاجية
- **شفافية كاملة:** تقدر تشوف كل مرحلة (تفكير → تخطيط → تسليم → تنفيذ → مراجعة) لحظياً مع أنيميشنات سلسة

### 🔄 نظام المراجعة والإصلاح التلقائي (3 طبقات)

#### الطبقة 1: المراجع التلقائي (`barq-reviewer`)
- بعد كل بناء يتم إرسال الملفات المولّدة إلى Gemini للمراجعة
- يتحقق من: اكتمال الملفات، دعم RTL، خط Cairo، المحتوى العربي، التصميم المتجاوب
- النتيجة: `approved` ✅ أو `needs_fix` 🔧
- إذا `needs_fix`: يولّد `fix_prompt` ويعيد إرساله للمبرمج تلقائياً
- **حد أقصى محاولتين** لمنع الحلقات اللانهائية

#### الطبقة 2: الإصلاح الذكي (`barq-fixer`)
- يكتشف أخطاء Runtime في المعاينة عبر `PreviewErrorBoundary`
- يحلل `errorMessage` و `componentStack` مع سياق VFS الكامل
- يقترح عمليات VFS (إنشاء/تعديل/حذف ملفات) عبر أداة `suggest_vfs_fixes`
- المستخدم يقبل أو يرفض الإصلاح

#### الطبقة 3: حدود الخطأ (Error Boundary)
- مكون `PreviewErrorBoundary` (React Class Component) يلتقط أخطاء العرض
- يعرض زر **"إعادة المحاولة"** وزر **"إصلاح تلقائي ⚡"**
- زر الإصلاح يستدعي `barq-fixer` مباشرة

### 💬 محادثة ذكية بالعربي
- **وضع التخطيط (Planning Mode):** الذكاء الاصطناعي يسأل أسئلة توضيحية قبل البناء
- **لهجة سعودية طبيعية:** System Prompt مخصص باللهجة السعودية
- **زر "ابدأ البناء ⚡":** لا يبدأ التوليد إلا عند تأكيد المستخدم بكلمة مثل "ابدأ" أو "يلا" أو "نفذ"
- **سجل محادثات محفوظ:** كل الرسائل تُحفظ في جدول `chat_messages` مرتبطة بالمشروع

### 📋 قوالب جاهزة (Prompt Templates)
- **10 قوالب سريعة** تظهر عند مشروع فارغ أو للمستخدمين الجدد
- مكون `PromptTemplates` — شبكة قابلة للتخصيص (compact/full)

### 📚 مكتبة القوالب الاحترافية (Templates Library — `/templates`)

صفحة مستقلة تحتوي **12 قالب احترافي** كامل مع بيانات تفصيلية:

| # | القالب | التصنيف | المكونات | الألوان |
|---|--------|---------|----------|---------|
| 1 | 🍽️ مطعم فاخر | مطاعم وكافيهات | 8 | ذهبي/بني |
| 2 | 🏥 عيادة طبية | صحة وطب | 9 | أخضر/كحلي |
| 3 | 🏢 شركة عقارات | عقارات وبناء | 10 | أزرق سماوي |
| 4 | 🛒 متجر إلكتروني | تجارة إلكترونية | 9 | بنفسجي |
| 5 | ⚖️ مكتب محاماة | قانون واستشارات | 9 | كحلي/ذهبي |
| 6 | ✂️ صالون تجميل | جمال وعناية | 9 | وردي |
| 7 | 💪 نادي رياضي | رياضة ولياقة | 9 | أحمر/برتقالي |
| 8 | 💻 شركة برمجيات | تقنية وبرمجة | 9 | أزرق/سيان |
| 9 | 🎓 أكاديمية تعليمية | تعليم وتدريب | 9 | بنفسجي |
| 10 | 📸 مصور فوتوغرافي | فنون وتصوير | 9 | ذهبي/أسود |
| 11 | 💼 شركة استشارات | استشارات | 9 | فيروزي |
| 12 | 🧑‍💻 موقع شخصي | مواقع شخصية | 9 | نيلي |

#### مميزات صفحة القوالب:
- **بحث نصي** في العنوان والوصف
- **فلترة حسب التصنيف** (10 تصنيفات) مع عداد لكل فئة
- **بطاقات تفاعلية** مع hover effects وتكبير الأيقونة
- **لوحة ألوان** مرئية لكل قالب
- **زر "استخدم هذا القالب"** → ينشئ مشروعاً ويبدأ البناء فوراً
- **حالة تحميل** (Loading State) أثناء إنشاء المشروع
- **قسم قوالب شهيرة** في الصفحة الرئيسية (أفضل 3 قوالب مميزة)
- **رابط "تصفح جميع القوالب"** في Navbar والصفحة الرئيسية

#### البنية التقنية:
```typescript
// src/lib/templates-data.ts
interface Template {
  id: string;
  title: string;
  description: string;
  category: TemplateCategory;
  icon: string;
  colors: { primary: string; secondary: string; accent: string };
  files: VFSFile[];
  pageCount: number;
  featured: boolean;
  prompt: string; // الوصف الكامل للبناء
}
```

### 🎛️ تحسينات واجهة المحادثة (Chat UX Enhancements)

#### 1. زر إيقاف البناء (Stop Generation)
- يظهر أثناء التنفيذ بجانب حقل الإدخال
- يستدعي `engine.handleAbort()` لإلغاء العملية فوراً
- أيقونة `Square` (■) بلون destructive واضح
- متاح على الجوال وسطح المكتب

#### 2. Typing Indicator ديناميكي
- مكون `DynamicTypingIndicator` يعرض رسائل متغيرة كل 2.5 ثانية:
  - "برق يفكر في التصميم..." · "يحلل طلبك بعناية..." · "يبني المكونات..."
  - "يختار الألوان المناسبة..." · "يراجع الجودة..." · "يكتب الكود..."
- رسائل مخصصة لوضع البناء vs وضع التفكير
- أنيميشن نقاط الكتابة مع أيقونة Bot متحركة

#### 3. زر تراجع (Undo Last Message)
- يظهر عند hover فوق آخر رسالة للمستخدم
- أيقونة `Trash2` صغيرة بلون destructive
- يحذف آخر رسالة مستخدم + الرد المرتبط بها
- لا يظهر أثناء البناء أو التفكير

#### 4. Smooth Scroll + Auto-scroll
- `scroll-smooth` على حاوية الرسائل
- `scrollIntoView({ behavior: "smooth" })` تلقائي عند كل رسالة جديدة
- ref مخصص لنقطة النهاية (`messagesEndRef`)

#### 5. Keyboard Shortcuts
| الاختصار | الوظيفة |
|----------|---------|
| `Enter` | إرسال الرسالة |
| `Ctrl+L` | مسح المحادثة بالكامل |
| `Esc` | إيقاف البناء/التفكير |
- مسجلة عبر `window.addEventListener("keydown")` مع cleanup
- تعمل في جميع أوضاع العرض (جوال + سطح مكتب)

#### 6. رسائل قابلة للتوسيع (Read More/Less)
- مكون `ExpandableMessage` — يقصر الرسائل الطويلة (> 300 حرف)
- زر "اقرأ المزيد ↓" / "اقرأ أقل ↑" بلون primary
- يعمل مع رسائل المستخدم والمساعد

### 🔧 تعديل تكراري ذكي (Context-Aware Refinement)
- يرسل أول 800 حرف من كل ملف (+ آخر 300 حرف للملفات الكبيرة) كسياق
- الذكاء يحلل الملفات الموجودة ويعدل المطلوب فقط
- يدعم إجراءات: `create` (إنشاء) و `update` (تعديل) و `delete` (حذف)
- مخطط الاعتماديات (Dependency Graph) يحدد العلاقات بين المكونات

### 📱 معاينة لحظية متجاوبة
- **محرك المعاينة (`preview-builder.ts`):** يحوّل JSX إلى HTML نظيف:
  - يزيل `import`/`export`/`function` declarations
  - يحوّل `className` → `class`
  - يحوّل `style={{...}}` إلى inline CSS
  - يزيل Event Handlers (`onClick`, `onChange`...)
  - يزيل `ref`, `key`, تعبيرات JSX الديناميكية
  - يدعم `.map()`, ternary operators, `&&` conditions
- **تجميع المكونات:** يبحث عن `App.tsx` ويستبدل `<Header />` بمحتوى `Header.tsx` الفعلي
- **ترتيب ذكي:** إذا لم يوجد App.tsx يرتب: Header → Hero → (باقي المكونات) → Contact → Footer
- **القالب النهائي:** HTML5 كامل مع Tailwind CDN + خط Cairo + RTL

#### 🎛️ أدوات المعاينة المتقدمة (Preview Panel Tools)

| الأداة | الوظيفة | الوصف |
|--------|---------|-------|
| 📋 نسخ الكود | Copy Code | زر نسخ بجانب كل ملف في VFS مع إشعار Toast |
| 🪟 نافذة جديدة | Open in New Window | فتح المعاينة في tab جديد للعرض الكامل |
| 📐 شبكة توجيهية | Grid Overlay | شبكة 8px قابلة للتشغيل/الإيقاف فوق المعاينة |
| 📏 مسطرة | Ruler | مساطر أفقية وعمودية بعلامات كل 50px |
| 🌙 الوضع الداكن | Dark Mode Toggle | تبديل dark/light للمعاينة فقط (يُحفظ في localStorage) |
| 📸 تحميل صورة | Screenshot/Export | تحويل المعاينة لصورة PNG أو HTML قابلة للتحميل |
| 🔄 تحديث تلقائي | Hot Reload | المعاينة تتحدث تلقائياً عند أي تعديل في VFS |

#### 📱 أحجام الأجهزة المسبقة (Device Presets)

| الجهاز | الأبعاد |
|--------|---------|
| Mobile | 375 × 667 |
| iPhone 14 Pro | 393 × 852 |
| Tablet | 768 × 1024 |
| iPad Pro 11" | 834 × 1194 |
| Desktop | 100% |
| MacBook Pro 14" | 1512 × 982 |

### 📂 نظام الملفات الافتراضي (Virtual File System — VFS)

#### البنية التقنية:
```typescript
interface VFSFile {
  name: string;        // مسار الملف (e.g., "Header.tsx")
  content: string;     // محتوى الملف الكامل
  language: 'tsx' | 'css' | 'html' | 'json' | 'ts' | 'js';
  version?: number;    // رقم الإصدار
}

interface VFSOperation {
  path: string;
  content?: string;
  action: 'create' | 'update' | 'delete';
}

interface VFSVersionSnapshot {
  version: number;
  timestamp: string;
  files: VFSFile[];
  message: string;     // وصف التغيير
}
```

#### المميزات:
- **تاريخ الإصدارات:** كل تغيير يُحفظ كـ Snapshot كامل
- **تراجع/إعادة (Undo/Redo):** التنقل بين الإصدارات السابقة
- **حفظ تلقائي:** يُحفظ في عمود `vfs_data` (JSON) بجدول `projects`
- **استعرض الكود:** مستعرض ملفات كامل في المتصفح

### 🎨 تصميم احترافي تلقائي

#### نظام الألوان الذكي:
| نوع المشروع | اللون الأساسي | اللون الثانوي | لون التمييز |
|-------------|---------------|---------------|-------------|
| مطاعم/طعام | amber-600 | stone-800 | amber-400 |
| تقنية/برمجيات | blue-600 | slate-800 | cyan-400 |
| صحة/طب | emerald-600 | slate-700 | green-400 |
| تعليم | violet-600 | slate-800 | purple-400 |
| عقارات | sky-600 | gray-800 | sky-400 |
| أزياء/جمال | rose-500 | gray-800 | pink-400 |
| عام/شركات | slate-700 | blue-600 | blue-400 |

#### معايير التصميم:
- **Hero Section:** `min-h-[90vh]` مع تدرجات وأشكال زخرفية
- **الأقسام:** padding `py-20 md:py-28 lg:py-32`
- **الأزرار الأساسية:** `px-8 py-4 rounded-xl font-bold shadow-lg hover:-translate-y-0.5 transition-all`
- **البطاقات:** `bg-white rounded-2xl p-8 shadow-sm hover:shadow-xl hover:-translate-y-1 transition-all`
- **العناوين:** `text-4xl sm:text-5xl md:text-6xl lg:text-7xl font-extrabold`

### 🏗️ بناء مشاريع كاملة (Full-Stack Scaffolding)

الحد الأدنى **6 ملفات** لكل مشروع:

| الملف | المحتوى | متطلبات |
|-------|---------|---------|
| `styles.css` | CSS variables, animations (fadeInUp, fadeIn, slideInRight), glass-effect | أنيميشنات + تدرجات |
| `Header.tsx` | Sticky nav, logo, links, CTA, mobile hamburger | glass-effect + border-b |
| `Hero.tsx` | Gradient/light background, heading, subtitle, 2 buttons, stats | min-h-[90vh] |
| `Services.tsx` | Badge + heading, 3-column grid with icon cards | bg-gray-50 |
| `About.tsx` | 2-column: text with bullets + visual gradient/pattern | — |
| `Contact.tsx` | Gradient bg, white form card, contact info | — |
| `Footer.tsx` | bg-gray-900, 3-4 columns, social icons, copyright | — |
| `App.tsx` | `<div dir="rtl" lang="ar" style={{fontFamily: 'Cairo'}}>` wrapping all | نقطة الدخول |

### 📤 تصدير GitHub
- OAuth App مع callback URL: `https://barqai.site/auth/github/callback`
- يصدّر كامل هيكل المشروع: `package.json`, `vite.config.ts`, `.env`, إلخ
- Edge Function مخصصة (`github-export`) تدعم:
  - `get_auth_url` — رابط المصادقة
  - `exchange_code` — تبادل الكود
  - `create_repo` — إنشاء Repository
  - `push_files` — رفع الملفات

### 📦 تصدير ZIP
- تحميل المشروع كملف مضغوط جاهز للتشغيل

### 🤝 التعاون والمشاركة (Collaboration & Sharing)

#### 1. مشاركة المعاينة العامة (Public Preview Link)
- توليد رابط فريد لكل مشروع (`/preview/:shareId`)
- المعاينة تعمل بدون تسجيل دخول
- تفعيل/تعطيل المشاركة من لوحة المشروع
- نسخ الرابط مع إشعار Toast

#### 2. نظام التعليقات (Comments System)
- إضافة تعليقات على المشروع مع timestamps
- تعديل وحذف التعليقات
- تحديثات لحظية عبر Supabase Realtime
- عرض في sidebar جانبي بلوحة المشروع

#### 3. نسخ المشروع (Clone Project)
- زر "نسخ المشروع" ينشئ نسخة كاملة من VFS + البيانات
- يفتح المشروع الجديد في Builder مباشرة

#### 4. تاريخ الإصدارات المحسّن (Version History UI)
- عرض timeline للإصدارات مع timestamps
- مقارنة بين إصدارين (ملفات جديدة/محذوفة/معدّلة)
- استعادة إصدار سابق بنقرة واحدة
- عرض عدد الملفات لكل إصدار

#### البنية التقنية:
```sql
-- أعمدة جديدة في جدول projects
share_enabled boolean DEFAULT false
share_id uuid UNIQUE DEFAULT gen_random_uuid()

-- جدول التعليقات
project_comments (id, project_id, user_id, content, created_at, updated_at)
-- مع RLS + Realtime مفعّل
```

### 👤 إدارة المستخدمين والمشاريع
- تسجيل دخول آمن بالبريد الإلكتروني (Supabase Auth)
- لوحة مشاريع لإدارة جميع المشاريع السابقة
- حفظ تلقائي مستمر للمشروع والمحادثات

### 💼 نموذج SaaS متكامل
- ثلاث باقات: **أساسي** · **احترافي** · **مؤسسات**
- نظام حدود استخدام يومية:
  - Planner: 50 طلب/يوم
  - Builder: 30 طلب/يوم
  - Reviewer: 30 طلب/يوم
- ملف تعريف العلامة التجارية (Brand Profile): اسم، شعار، ألوان

### 🔒 أمان على مستوى المؤسسات
- مصادقة JWT في كل Edge Function
- `check_and_increment_usage` — دالة PL/pgSQL لفحص الحدود وزيادة العداد ذرياً (Atomic)
- سياسات RLS على جميع الجداول
- Service Role Key للعمليات الإدارية فقط

### 🛡️ نظام الاستقرار والتعافي (Stability System)

#### 1. Error Boundary شامل (Global Error Boundary)
- مكون `GlobalErrorBoundary` يلتف حول التطبيق بالكامل في `main.tsx`
- يلتقط أي خطأ غير متوقع في أي مكان بالتطبيق
- يعرض واجهة أنيقة بالعربي مع:
  - رسالة خطأ واضحة ومفهومة
  - تفاصيل تقنية (اختياري) بخط mono
  - زر **"إعادة تحميل"** — `window.location.reload()`
  - زر **"الرئيسية"** — `window.location.href = "/"`
- يستخدم inline styles (لا يعتمد على CSS خارجي قد يكون سبب الخطأ)

#### 2. نظام Retry التلقائي (Auto-Retry with Backoff)
- `fetchWithRetry()` — wrapper ذكي لكل طلبات API
- **3 محاولات** مع Progressive Backoff:
  - المحاولة 1: فوري
  - المحاولة 2: بعد 2 ثانية
  - المحاولة 3: بعد 4 ثوانٍ
- يعيد المحاولة تلقائياً عند:
  - `429` — Rate Limit
  - `502`, `503`, `504` — Server Errors
  - Network Errors (انقطاع الاتصال)
- **لا يعيد** المحاولة عند:
  - `AbortError` — إلغاء يدوي
  - أخطاء المصادقة
- **Timeout:** تمت زيادته من 45 ثانية إلى **60 ثانية**

#### 3. Skeleton Loaders
- `ChatSkeleton` — هيكل تحميل للمحادثات (3 فقاعات متبادلة)
- `ProjectsSkeleton` — شبكة بطاقات مشاريع وهمية (6 بطاقات)
- `BuilderSkeleton` — هيكل لوحة البناء
- `InlineLoader` — مؤشر تحميل سطري مع نص عربي
- مستخدمة في: صفحة المشاريع، صفحة البيلدر (تحميل الرسائل)

#### 4. معالجة أخطاء API ذكية
- تصنيف الأخطاء حسب النوع:
  | نوع الخطأ | الرسالة | نوع Toast |
  |-----------|---------|-----------|
  | انقطاع اتصال | "انقطع الاتصال بالخدمة" | ⚠️ warning |
  | انتهاء جلسة | "انتهت جلسة تسجيل الدخول" | ❌ error |
  | حد يومي | "وصلت للحد اليومي" | ❌ error |
  | خطأ عام | تفاصيل الخطأ | ❌ error |
  | إلغاء بناء | "تم إيقاف البناء" | ⚠️ warning |
  | نجاح بناء | "تم تحديث النظام بنجاح" | ✅ success |

#### 5. Toast Notifications (Sonner)
- إشعارات لكل العمليات بالعربي:
  - ✅ نجاح: حفظ، بناء، إصلاح، حذف
  - ❌ فشل: اتصال، مصادقة، حد استخدام
  - ⚠️ تحذير: إلغاء، انقطاع
  - ℹ️ معلومة: رفض إصلاح، إلغاء عملية

### 🖥️ واجهة المستخدم — ThinkingEngine

مكون `ThinkingEngine.tsx` يعرض خط الأنبوب بالكامل:

```
[🧠 تفكير] ──→ [📋 تخطيط] ──→ [🔀 تسليم] ──→ [🏗️ تنفيذ] ──→ [🔍 مراجعة]
```

#### المراحل (PipelineStage):
```typescript
type PipelineStage = 'thinking' | 'planning' | 'handoff' | 'building' | 'reviewing' | 'done';
```

| المرحلة | الأيقونة | اللون | الوصف |
|---------|---------|-------|-------|
| `thinking` | Brain | purple-400 | برق يفكر... |
| `planning` | Zap | accent (gold) | يخطط البنية... |
| `handoff` | ArrowLeftRight | blue-400 | يرسل التعليمات للمبرمج... |
| `building` | Hammer | amber-400 | المبرمج ينفذ... |
| `reviewing` | ShieldCheck | emerald-400 | المراجع يتحقق... |
| `done` | Sparkles | green-400 | اكتمل! ✨ |

#### الأنيميشنات:
- **Ping Effect:** نبض على الأيقونة النشطة
- **Stage Transition:** تأثير scale + fade عند الانتقال بين المراحل
- **Connector Lines:** خطوط متحركة تملأ تدريجياً مع تقدم المراحل
- **Typing Dots:** نقاط متحركة بجانب المرحلة النشطة
- **Staggered Entry:** ظهور متتابع للعناصر بفارق `60ms`
- **Handoff Panel:** يعرض التعليمات التقنية بخط `mono` في إطار أزرق

### 📱 تصميم متجاوب بالكامل
- **سطح المكتب:** لوحات قابلة لتغيير الحجم (`react-resizable-panels`)
- **الجوال:** عرض ملء الشاشة مع شريط تنقل سفلي
- تبديل بين: **المحادثة** · **المعاينة** · **الكود**

### ⚡ تحسينات الأداء (Performance Optimizations)

#### 1. Lazy Loading + Code Splitting
- جميع الصفحات محملة بـ `React.lazy()` + `Suspense`
- كل صفحة في chunk منفصل (BuilderPage, ProjectsPage, LandingPage, إلخ)
- `PageLoader` — مكون fallback أنيق بالعربي أثناء تحميل الصفحات
- يقلل حجم الـ initial bundle بشكل كبير

#### 2. Web Worker لمحرك المعاينة
- `preview-builder.worker.ts` — ينقل منطق JSX→HTML إلى thread منفصل
- `usePreviewWorker` hook — واجهة سلسة مع fallback للـ main thread
- لا يعطل واجهة المستخدم أثناء بناء المعاينة (non-blocking)
- يدعم الإلغاء عبر `pendingId` pattern

#### 3. TanStack Query Optimization
- `staleTime: 5 دقائق` — يقلل طلبات API المتكررة
- `gcTime: 10 دقائق` — يحتفظ بالبيانات في الذاكرة
- `retry: 2` — إعادة محاولة تلقائية
- `refetchOnWindowFocus: false` — لا يعيد التحميل عند التركيز

#### 4. Navigation Progress Bar
- شريط تقدم أزرق رفيع في أعلى الصفحة عند التنقل
- `NavigationProgress` — مكون Pure CSS بدون مكتبات خارجية
- أنيميشن: 0% → 80% (400ms) → 100% (200ms) → fade out
- يتتبع `location.pathname` تلقائياً

#### 5. Image Lazy Loading
- جميع `<iframe>` تستخدم `loading="lazy"`
- صور المشاريع في ProjectsPage محملة lazily
- Skeleton loaders أثناء تحميل المحتوى

---

## 🛠 التقنيات المستخدمة

### الواجهة الأمامية (Frontend)

| التقنية | الإصدار | الاستخدام |
|---------|---------|-----------|
| **React** | 18.3 | إطار العمل الرئيسي |
| **TypeScript** | — | نظام الأنواع الثابت |
| **Vite** | — | أداة البناء (HMR سريع) |
| **Tailwind CSS** | — | Utility-first CSS framework |
| **Shadcn/UI** | — | مكونات Radix UI مخصصة |
| **React Router** | v6 | التوجيه مع AuthGuard |
| **TanStack React Query** | v5 | إدارة حالة البيانات والتخزين المؤقت |
| **React Resizable Panels** | — | Split panels للبيلدر |
| **Lucide React** | — | مكتبة أيقونات SVG |
| **Sonner** | — | إشعارات Toast عربية |
| **Recharts** | — | الرسوم البيانية |
| **Zod** | — | التحقق من البيانات |
| **React Hook Form** | — | إدارة النماذج |
| **date-fns** | — | معالجة التواريخ |
| **class-variance-authority** | — | Component variants |
| **tailwind-merge** | — | دمج Tailwind classes |
| **tailwindcss-animate** | — | أنيميشنات Tailwind |
| **cmdk** | — | Command palette |
| **vaul** | — | Drawer component |
| **embla-carousel-react** | — | Carousel |
| **input-otp** | — | OTP input |

### الخلفية (Backend)

| التقنية | الاستخدام |
|---------|-----------|
| **Supabase** | BaaS: قاعدة بيانات + مصادقة + تخزين |
| **PostgreSQL** | قاعدة البيانات العلائقية |
| **Edge Functions (Deno)** | 5 دوال سحابية متخصصة |
| **Row Level Security (RLS)** | حماية البيانات |
| **PL/pgSQL** | `check_and_increment_usage()` |
| **JWT Authentication** | مصادقة كل طلب |

### نماذج الذكاء الاصطناعي (AI Models)

| النموذج | المزود | الدور | الـ Endpoint |
|---------|--------|------|-------------|
| **Gemini 2.5 Flash** | Google | المشرف (Planner) | `generativelanguage.googleapis.com/v1beta/openai/chat/completions` |
| **Gemini 2.5 Flash** | Google | المراجع (Reviewer) | نفس الـ endpoint |
| **Gemini 2.5 Flash** | Google | المصلح (Fixer) | نفس الـ endpoint |
| **LLaMA 3.3 70B Versatile** | Groq | المبرمج (Builder) | `api.groq.com/openai/v1/chat/completions` |
| **LLaMA 3.3 70B Versatile** | Groq | Fallback لجميع المحركات | نفس الـ endpoint |

### البنية التحتية (Infrastructure)

| التقنية | الاستخدام |
|---------|-----------|
| **Lovable Cloud** | الاستضافة والنشر |
| **SSE (Server-Sent Events)** | البث اللحظي ثنائي الاتجاه |
| **GitHub OAuth App** | تصدير المشاريع |
| **Tailwind CDN** | تحميل Tailwind في المعاينة |
| **Google Fonts CDN** | خط Cairo في المعاينة |

---

## ⚙️ طريقة العمل بالتفصيل

### الخطوة 1: المستخدم يرسل رسالة

```
"أبغى موقع لمطعم سعودي"
```

الرسالة تُحفظ في `chat_messages` وتُرسل مع سياق VFS إلى `barq-planner`.

### الخطوة 2: المشرف يفكر (barq-planner)

**System Prompt:**
```
أنت "برق" ⚡ — مهندس حلول ذكاء اصطناعي خبير في Barq AI.
- تتكلم باللهجة السعودية بشكل طبيعي ومحترف
- تحلل طلبات المستخدم وتحولها إلى خطة بناء تكرارية
- التخطيط قبل التنفيذ: لا تستدعي أداة prepare_build_prompt إلا بعد موافقة المستخدم
```

**الأداة المتاحة:** `prepare_build_prompt`
```json
{
  "build_prompt": "English technical prompt for builder...",
  "summary_ar": "ملخص عربي للمستخدم",
  "project_name": "اسم المشروع",
  "dependency_graph": {
    "nodes": [{"id": "Header.tsx", "action": "create"}, ...],
    "edges": [{"from": "App.tsx", "to": "Header.tsx"}, ...]
  }
}
```

**السلوك:**
- إذا VFS فارغ → يسأل عن التفاصيل (بناء جديد)
- إذا VFS موجود → يحلل الملفات ويقترح تعديلات مستهدفة
- ينتظر كلمة تأكيد ("ابدأ", "يلا", "نفذ") قبل استدعاء الأداة

### الخطوة 3: التسليم (Handoff)

المشرف يولّد `build_prompt` بالإنجليزية ويُرسل كحدث SSE:
```json
{"event": "build_ready", "prompt": "...", "summary": "...", "dependencyGraph": {...}}
```

UI يعرض التعليمات في إطار أزرق بخط `mono` مع عنوان "تعليمات المشرف → المبرمج".

### الخطوة 4: المبرمج ينفذ (barq-chat)

**System Prompt:** (106 سطر) يشمل:
- قواعد صارمة: عربي 100%, RTL, Cairo font, SVG inline icons
- نظام ألوان حسب نوع المشروع
- معايير التصميم: spacing, typography, buttons, cards
- الحد الأدنى 6 ملفات
- وضع التعديل: يقرأ `EXISTING FILES` ويعدل المطلوب فقط

**الأداة:** `generate_website`
```json
{
  "thought_process": ["خطوة 1...", "خطوة 2..."],
  "design_personality": "formal|creative|minimalist|bold",
  "vfs_operations": [
    {"path": "Header.tsx", "action": "create", "content": "...", "language": "tsx"}
  ],
  "user_message": "تم بناء الموقع بنجاح! ⚡",
  "css_variables": {"primary_color": "...", "border_radius": "..."}
}
```

**البث (Streaming):**
```
thinking_start → thinking_step × N → file_start → file_done × N → done
```

### الخطوة 5: تحديث VFS

الملفات تُطبق على VFS عبر `applyVFSOperations`:
1. مقارنة كل `path` بالملفات الموجودة
2. تنفيذ create/update/delete
3. إنشاء VFS Snapshot جديد
4. حفظ في Supabase (`projects.vfs_data`)

### الخطوة 6: بناء المعاينة (Preview Engine)

```typescript
buildPreviewHTML(files) → HTML string → iframe.srcdoc
```

**خطوات التحويل:**
1. فصل ملفات CSS عن TSX
2. البحث عن `App.tsx` كهيكل رئيسي
3. تحويل كل مكون من JSX إلى HTML نظيف (`jsxToHTML`)
4. استبدال `<Header />` في App.tsx بمحتوى Header.tsx
5. تجميع CSS في `<style>` tag
6. إنتاج HTML5 كامل مع Tailwind CDN + Cairo font

### الخطوة 7: المراجعة التلقائية (barq-reviewer)

يُرسل `build_prompt` + الملفات المولّدة إلى Gemini:
```json
{
  "status": "needs_fix",
  "summary_ar": "وجدت 3 مشاكل...",
  "issues": [
    {"file": "Footer.tsx", "issue": "محتوى غير عربي", "fix_instruction": "..."}
  ],
  "fix_prompt": "English prompt to fix all issues..."
}
```

**إذا `needs_fix`:**
- `fixAttemptsRef` يتتبع عدد المحاولات
- إذا < 2: يعيد إرسال `fix_prompt` إلى المشرف → المبرمج → المراجع
- إذا >= 2: يتوقف ويعرض المشاكل للمستخدم

**إذا `approved`:**
- يعرض ✅ مع ملخص عربي
- يختفي بعد 4 ثوانٍ

---

## 🔑 نظام المفاتيح والـ Failover

### استراتيجية التبديل (High Availability):

```
طلب جديد
    │
    ▼
┌─────────────────┐
│  Gemini Key #1  │ ← يجرب أولاً
└────────┬────────┘
         │ 429 Rate Limit?
         ▼
┌─────────────────┐
│  Gemini Key #2  │ ← يجرب ثانياً
└────────┬────────┘
         │ 429 Rate Limit?
         ▼
┌─────────────────┐
│   Groq Key #1   │ ← Fallback
└────────┬────────┘
         │ 429?
         ▼
┌─────────────────┐
│   Groq Key #2   │
└────────┬────────┘
         │ 429?
         ▼
┌─────────────────┐
│   Groq Key #3   │
└────────┬────────┘
         │ فشل الكل?
         ▼
  ❌ "جميع المفاتيح وصلت للحد"
```

### المفاتيح المطلوبة (Supabase Secrets):

| المفتاح | المحرك | ملاحظات |
|---------|--------|---------|
| `GEMINI_API_KEY` | Planner, Reviewer, Fixer | أساسي |
| `GEMINI_API_KEY_2` | Fallback | احتياطي |
| `GROQ_API_KEY` | Builder + Fallback | أساسي |
| `GROQ_API_KEY_2` | Fallback | احتياطي |
| `GROQ_API_KEY_3` | Fallback | احتياطي |

---

## 📡 بروتوكول الاتصال (SSE Events)

جميع الاتصالات بين Frontend و Edge Functions تتم عبر **Server-Sent Events**:

### أحداث barq-planner:
| الحدث | البيانات | الوصف |
|-------|---------|-------|
| `message_delta` | `{content}` | نص تدريجي من المشرف |
| `build_ready` | `{prompt, summary, projectName, dependencyGraph}` | خطة البناء جاهزة |
| `done` | — | انتهى البث |

### أحداث barq-chat (Builder):
| الحدث | البيانات | الوصف |
|-------|---------|-------|
| `thinking_start` | — | بداية التفكير |
| `thinking_step` | `{step}` | خطوة تفكير واحدة |
| `file_start` | `{path, action, language}` | بداية توليد ملف |
| `file_done` | `{path, content}` | ملف مكتمل |
| `message_delta` | `{content}` | رسالة نصية |
| `done` | — | انتهى البث |

### أحداث barq-fixer:
| الحدث | البيانات | الوصف |
|-------|---------|-------|
| `fix_ready` | `{operations, summary}` | اقتراحات الإصلاح |
| `done` | — | انتهى البث |

### معالجة SSE في Frontend (`barq-api.ts`):
- **Timeout:** 60 ثانية بدون نشاط → إلغاء (محدّث من 45 ثانية)
- **Auto-Retry:** `fetchWithRetry()` — 3 محاولات مع Progressive Backoff
- **Rate Limit Recovery:** إعادة محاولة تلقائية عند 429/502/503/504
- **Line-by-line parsing:** معالجة سطر بسطر (ليس `\n\n`)
- **Final flush:** معالجة أي بيانات متبقية في Buffer
- **Error recovery:** تخطي أسطر JSON غير صالحة

---

## 🗄️ هيكل قاعدة البيانات

| الجدول | الأعمدة الرئيسية | RLS | الوصف |
|--------|-----------------|-----|-------|
| `profiles` | `user_id`, `display_name`, `avatar_url` | ✅ | بيانات المستخدمين |
| `projects` | `user_id`, `title`, `vfs_data` (JSON), `preview_html` | ✅ | المشاريع + VFS |
| `chat_messages` | `project_id`, `user_id`, `role`, `content` | ✅ | سجل المحادثات |
| `brand_profiles` | `user_id`, `brand_name`, `primary_color`, `logo_url` | ✅ | العلامة التجارية |
| `user_usage` | `user_id`, `usage_date`, `builder_calls`, `planner_calls`, `reviewer_calls` | ✅ | تتبع الاستخدام |

### الدوال (Functions):
- `check_and_increment_usage(p_user_id, p_function_type, p_daily_limit)` → `boolean`
  - فحص ذري: إذا الاستخدام < الحد → يزيد العداد ويرجع `true`
  - وإلا → يرجع `false`

---

## 📁 هيكل المشروع الكامل

```
src/
├── assets/
│   ├── logo-light.jpg           # شعار BARQ Ai
│   └── logo-light-full.png      # شعار كامل
├── components/
│   ├── ui/                      # 40+ مكون Shadcn/UI
│   ├── v2/
│   │   ├── PreviewErrorBoundary.tsx  # التقاط أخطاء المعاينة
│   │   └── PreviewPanel.tsx         # لوحة المعاينة
│   ├── ThinkingEngine.tsx       # عرض خط الأنبوب المتحرك
│   ├── PromptTemplates.tsx      # 10 قوالب مشاريع جاهزة
│   ├── ExpandableMessage.tsx    # رسائل قابلة للتوسيع (اقرأ المزيد/أقل)
│   ├── DynamicTypingIndicator.tsx # مؤشر كتابة ديناميكي
│   ├── ChatPage.tsx             # واجهة المحادثة
│   ├── ChatSidebar.tsx          # الشريط الجانبي
│   ├── MobilePreview.tsx        # معاينة الجوال
│   ├── AppSidebar.tsx           # الشريط الجانبي الرئيسي
│   ├── BarqLogo.tsx             # مكون الشعار
│   ├── GitHubExportModal.tsx    # نافذة تصدير GitHub
│   ├── ActivityLog.tsx          # سجل النشاط
│   └── NavLink.tsx              # رابط التنقل
├── hooks/
│   ├── v2/
│   │   ├── useBuildEngine.ts    # المحرك الرئيسي (455 سطر)
│   │   │   ├── handleSendMessage()   # Phase 1: التخطيط
│   │   │   ├── handleStartBuild()    # Phase 2: التنفيذ + المراجعة
│   │   │   ├── handleFixError()      # Phase 3: الإصلاح
│   │   │   ├── handleApplyFix()      # تطبيق الإصلاح
│   │   │   └── handleRejectFix()     # رفض الإصلاح
│   │   ├── useBuilderChat.ts    # إدارة المحادثة والحالة
│   │   └── useVFS.ts            # نظام الملفات + التاريخ
│   ├── useBuilderProject.ts     # إدارة المشروع
│   ├── useGitHubExport.ts       # تصدير GitHub
│   └── use-mobile.tsx           # كشف الجوال
├── lib/
│   ├── barq-api.ts              # واجهة API (311 سطر)
│   │   ├── processSSEStream()        # معالج SSE موحد
│   │   ├── streamBarqPlanner()       # بث المشرف
│   │   ├── streamBarqBuilder()       # بث المبرمج
│   │   ├── reviewBuild()             # مراجعة (non-streaming)
│   │   ├── streamBarqFixer()         # بث المصلح
│   │   └── githubExportAction()      # عمليات GitHub
│   ├── preview-builder.ts       # محرك المعاينة (220 سطر)
│   │   ├── jsxToHTML()               # تحويل JSX → HTML
│   │   └── buildPreviewHTML()        # تجميع المعاينة
│   └── utils.ts                 # cn() helper
├── pages/
│   ├── BuilderPage.tsx          # صفحة البناء الرئيسية
│   ├── ProjectsPage.tsx         # لوحة المشاريع
│   ├── ProjectDashboardPage.tsx # لوحة تحكم المشروع
│   ├── LandingPage.tsx          # الصفحة الرئيسية
│   ├── AuthPage.tsx             # تسجيل الدخول
│   ├── PricingPage.tsx          # الأسعار والباقات
│   ├── BrandSettingsPage.tsx    # إعدادات العلامة التجارية
│   ├── PrivacyPage.tsx          # سياسة الخصوصية
│   ├── TermsPage.tsx            # الشروط والأحكام
│   └── NotFound.tsx             # صفحة 404
└── integrations/
    └── supabase/
        ├── client.ts            # Supabase Client (تلقائي)
        └── types.ts             # Database Types (تلقائي)

supabase/
└── functions/
    ├── barq-planner/index.ts    # المشرف — 288 سطر
    ├── barq-chat/index.ts       # المبرمج — 421 سطر
    ├── barq-reviewer/index.ts   # المراجع — 269 سطر
    ├── barq-fixer/index.ts      # المصلح — 271 سطر
    └── github-export/index.ts   # تصدير GitHub
```

---

## 🎨 الهوية البصرية ونظام التصميم

### Design Tokens (`index.css`):

| Token | Light Mode | Dark Mode |
|-------|-----------|-----------|
| `--background` | `0 0% 100%` (أبيض) | `0 0% 0%` (أسود) |
| `--primary` | `205 100% 50%` (أزرق كهربائي) | `205 100% 55%` |
| `--accent` | `45 100% 50%` (ذهبي) | `45 100% 55%` |
| `--barq-electric` | `205 100% 50%` | `205 100% 55%` |
| `--barq-gold` | `45 100% 50%` | `45 100% 55%` |
| `--barq-glow` | `205 100% 60%` | `205 100% 65%` |
| `--barq-surface` | `0 0% 96%` | `0 0% 7%` |
| `--barq-gradient-electric` | `135deg, primary → 220°` | — |
| `--barq-shadow-glow` | `0 0 20px primary/0.15` | `0 0 20px primary/0.3` |

### أنيميشنات مخصصة:

| الاسم | المدة | الاستخدام |
|-------|------|-----------|
| `pulse-glow` | 2s | توهج الأزرار والأيقونات |
| `lightning-flash` | 1.5s | وميض البرق |
| `slide-up-fade` | 0.3s | ظهور العناصر من الأسفل |
| `typing-dot` | 1.4s | نقاط الكتابة المتحركة |

### الخطوط:
- **Cairo** — الخط الأساسي (Google Fonts)
- الأوزان: 300, 400, 500, 600, 700, 800, 900
- الاتجاه: RTL بالكامل (`direction: rtl`)

---

## 🚀 البدء السريع

```bash
# تثبيت المتطلبات
npm install

# تشغيل بيئة التطوير
npm run dev

# بناء للإنتاج
npm run build
```

---

## 📄 الترخيص

جميع الحقوق محفوظة © 2025 BARQ Ai

---

<p align="center">
  صُنع بـ ⚡ في السعودية 🇸🇦
</p>
